# Task

I want to be able to drag and drop BOS files into the viewer and they should open. Add that feature.

## Questions/Assumptions

- When a new .BOS is dropped while a model is loaded, replace the current model (user confirmed).
- Drag-drop enabled by default via viewer settings; stateful examples opt out.
- Drop zone is the viewer canvas.
